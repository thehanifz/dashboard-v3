// SHIM — file ini akan dihapus setelah seluruh import sudah diupdate ke hooks/
// Gunakan: import { useTablePagination } from "../../hooks/useTablePagination";
export { useTablePagination } from "../../hooks/useTablePagination";
